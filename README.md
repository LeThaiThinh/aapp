# app
 Le Thai Thinh 18021228
 app có thể thay đổi giữa các màn hình(routing/navigation)
 <image src="AppImage/en-about.png"></image>
 app có thể điều chỉnh ngôn ngữ(interionalization)
 <image src="AppImage/en-home.png"></image>
 <image src="AppImage/vn-home.png"></image>
 app có thể tải ảnh từ firestore:
 ảnh trước khi tải được dữ liệu từ firestore về(mất khoảng thời gian rất nhỏ):
    <image src="AppImage/en-firebase-notdone.png"></image>
 ảnh sau khi tải thành công dữ liệu:
    <image src="AppImage/en-firebase.png"></image>
